<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 背景  gradient是渐变,corners定义的是圆角 -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="10dp" />

            <solid android:color="#ffffff" />

            <stroke
                android:width="0.5dp"
                android:color="@color/gengduo_divider_color" />
        </shape>
    </item>

    <!-- 进度条颜色 -->
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="10dip" />

                <gradient
                    android:angle="90.0"
                    android:centerColor="@color/titlebar_bg_color"
                    android:centerY="0.45"
                    android:endColor="@color/titlebar_bg_color"
                    android:startColor="@color/titlebar_bg_color" />
            </shape>
        </clip>
    </item>
    <!-- 进度条 -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="10dip" />

                <solid android:color="@color/titlebar_bg_color" />
            </shape>
        </clip>
    </item>
</layer-list>